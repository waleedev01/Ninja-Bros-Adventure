package game.objects.platforms;

import city.cs.engine.Shape;
import city.cs.engine.World;

/**
 * Class used to make Walls
 */
public class RightWall extends Platform {
    public RightWall(World w, Shape s) {
        super(w, s);
    }
}
